# Optional. Without this key the project runs in deterministic extractive mode.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_JUDGE_MODEL=gpt-4o-mini

# Optional. Default is live Stack Exchange API; set cache for offline reproducibility.
STACKOVERFLOW_MODE=live
